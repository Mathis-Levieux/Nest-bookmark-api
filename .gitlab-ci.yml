image: docker:dind

services:
  - name: docker:dind

stages:
  - build

build:
  stage: build
  script:
    - docker build -t skurt .
    - docker compose up -d
    - docker exec -it skurt npm run test:docker:e2e
