image: docker:dind

services:
  - name: docker:dind

stages:
  - build
  - test

build:
  stage: build
  script:
    - docker build -t my-image .

test:
  stage: test
  script:
    - docker-compose up -d
    - docker-compose -f docker-compose.yml exec nest-test npm run test:e2e
